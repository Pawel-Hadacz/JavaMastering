package mastering.repetition;

import java.util.ArrayList;

public class DigPow {
    public static long digPow(int n, int p) {
        /*int start = 1;
        double x = (Math.pow(9, p));
        double y = (Math.pow(9, p + 1));
        ArrayList<Integer> array = new ArrayList<Integer>();
        for(int c: array)){
            array.add();
        }
        for (int i = 0; i < array; i++) {
            for (int j = 1; j < 10; j++) {
                array[i] = j;
               // System.out.println(array[i]);
            }

            System.out.println("");
        }
        for(int i = 0;i < p; i++ ){
            System.out.println(array[i]);
        }*/
        return 1;
     /*  if(x + y == n) {
           return p;
       }else
           {
            return -1;
            }
         }*/
    }
}
